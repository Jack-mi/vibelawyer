# vibelawyer 迭代进度（对标律师版 + FastMCP 服务化）

> 更新时点：2026-08-01。本文档跟踪两项并行工作：
> **A. 按 `output/对照分析-律师版vsAgent版.md` 的差距清单迭代整条链路**；
> **B. 用 FastMCP 把系统封装为可对外部署的 MCP Server**。

> **2026-08-01 续作（本会话完成剩余 5 项）**：
> 1. `docx_notes.py` 渲染升级 —— 冒烟验证通过（`scripts/smoke_render.py` 断言 11 项结构：TOC/一行式标题/逐字全文/fact_group 分组/文号列/流水子表/量刑情节/资金勾稽表/来源引用）。
> 2. `xlsx_catalog.py` 升级 —— 新增「分卷总览」sheet（每卷一行 + `P起-止:文件名` 索引）；案件信息 sheet 增补资金勾稽 6 项 + 流水笔数。
> 3. `mcp_server.py` FastMCP 封装 —— 25 个工具（19 passthrough 复用 `tools.py` handler + 6 专有），create_case/list_volumes/get_case_status 功能冒烟通过。
> 4. 打包与文档 —— `pyproject.toml` 修正 `pymupdf`→`pypdfium2`、补 `Pillow/numpy/fastmcp>=3.4.4,<4`、加 `vibelawyer-mcp` 入口；README 补 MCP Server 用法（stdio/http/StaticTokenVerifier/JWTVerifier）；CLAUDE.md 补 fastmcp 依赖与 MCP/smoke 命令。
> 5. 冒烟验证 —— 全模块 `py_compile` 通过；合成 workspace 渲染 docx+xlsx 断言通过；FastMCP app 实例化并列出 25 工具清单。
> **环境澄清**：conda base python（`/opt/homebrew/Caskroom/miniconda/base/bin/python`）已含全部依赖（fastmcp 3.4.4 / docx / openpyxl / pypdfium2 / claude_agent_sdk），CLAUDE.md 所述路径**有效**；`.venv` site-packages 为空、不可用，此前「路径失效」判断有误。

---

## 一、已完成

### 0. 前置成果
- **对照分析**：`output/对照分析-律师版vsAgent版.md` —— 律师版阅卷笔录/卷宗目录逐项解剖，P0/P1/P2 差距清单（摘要 vs 逐字全文、书证未按待证事实分组、无逐笔流水、无金额勾稽、缺文号、无目录等）。
- **环境**：新建 `.venv`（Python 3.12，uv 创建），已装 pypdfium2 / python-docx / openpyxl / Pillow / numpy / **fastmcp 3.4.5** / claude-agent-sdk。注意：CLAUDE.md 所述 conda base 路径（`/opt/homebrew/Caskroom/miniconda/...`）已失效，base 环境为 Python 3.9 且无依赖——文档待更新。
- **FastMCP 技术选型**（librarian 调研，已核实 v3.4.5 源码）：
  - 当前稳定版 **3.4.5**（非 2.x；4.0 在 beta），钉版 `fastmcp>=3.4.5,<4`；
  - 部署用 `mcp.run(transport="http", host=..., port=...)`，客户端连 `http://host:port/mcp`；SSE 已废弃；
  - 长任务采用 **job_id + 状态轮询** 模式（客户端无关性最好；4.0 的 TasksExtension 后续再迁）；
  - 文件下发用 tool 返回 `fastmcp.utilities.types.File` 或 resource 返回 bytes；
  - 共享状态用 lifespan 或模块级注册表（本实现选后者，`sessions.py`）。

### 1. 数据模型 `vibelawyer/workspace.py`（已编译通过）
- 新增 `Transaction`（日期/付款方/收款方/金额/账户/备注/页码）——对应差距 #3 逐笔流水。
- 新增 `FundsSummary`（报案合计/合同合计/指控金额/已返还/违法所得/已退赔/勾稽说明）——对应差距 #4 金额勾稽。
- `Statement` +`full_text`（逐字问答全文）、+`occasion`（笔录场景）——对应差距 #1。
- `ProceduralDoc` +`doc_no`（文号）、+`fact_group`；`DocumentaryEvidence` +`doc_no`、+`fact_group`、+`transactions`——对应差距 #2/#5。
- `IndictmentInfo` +`sentencing_circumstances`（量刑情节：从犯/认罪认罚等）——对应差距 #9。
- `CaseWorkspace` +`funds`；新增 `attach_transaction()`（按书证名匹配挂载，即时校验流水页码落在书证区间内）；`validate_all()` 增加流水页码复核；`to_dict()` 含 funds；新增 `set_workspace()` 供会话绑定。

### 2. 会话注册表 `vibelawyer/sessions.py`（新增文件，已编译通过）
- `CaseSession`：把 cfg + VolumeStore + CaseWorkspace + job 状态（status/step/log/result/error）打包，按 `case_id` 隔离——MCP 多案件的基础。
- `SessionManager` / 全局 `MANAGER`：create（建卷宗存储+登记页数，**不**预转换 docling）、`ensure_docling_cache`（惰性）、get/list/running_session。
- 明确的 v1 并发约束：**全流程 job 运行期间，其他案件的交互式调用将被拒绝**（进程内 SDK 工具依赖模块级活动绑定，切会串号）；同案件查询不受影响。

### 3. 工具层 `vibelawyer/tools.py`（已编译通过）
- 状态管理重构：`_STORE/_CFG` 全局变量 → **活动会话绑定**（`bind_session` / `active_session`）；`init_tools` 改为经 `MANAGER.create` 建会话再绑定——本地 CLI 行为不变，MCP 可按 case_id 切换。
- `record_statement` +`occasion`/`full_text`（工具描述明确"仅登记摘要视为未完成"）。
- `record_procedural_doc` +`doc_no`/`fact_group`；`record_documentary_evidence` +`doc_no`/`fact_group`。
- `record_indictment` +`sentencing_circumstances`。
- 新增 `add_transaction`（流水逐笔登记，挂书证、页码越界即报错）；新增 `record_funds_summary`。
- `get_workspace_summary` 增强：显示全文份数、文号份数、流水笔数、资金勾稽进度（供编排器核实）。
- 新工具已注册进 `get_tools()`。

### 4. 子 agent 指令 `vibelawyer/agents.py` + 编排 `vibelawyer/orchestrator.py`
- 供述/证言三个提取员：**强制 full_text 逐字转录问答**（"问：…／答：…"原顺序、不概括不删减；一份笔录一次调用）。
- 程序性文书提取员：**逐份提取文号**（程序合法性审查抓手）。
- 书证提取员：**fact_group 按待证事实分组**；流水类书证必须 add_transaction 逐笔登记。
- 起诉书阅读员：提取量刑情节；结论员：**比对各次供述全文找矛盾** + 计算并登记资金勾稽（口径不一致须 note 说明）。
- orchestrator 委派任务描述同步更新；`run_case` 新增 `session=`（复用已建会话，MCP 模式不重置工作区）与 `progress=` 步骤回调（供 job 状态流式上报）。

---

## 二、未完成（按执行顺序）

| # | 任务 | 要点 |
|---|---|---|
| 1 | **`generators/docx_notes.py` 渲染升级** | ① 标题后插自动目录 TOC field；② 供述块改律师式一行标题（`{人} 第N次 《卷》P起-止（有无同步录音录像/场景）`）+ 要点 + **全文渲染**；③ 第六/七部分按 `fact_group` 分组、表格加**文号列**；④ 书证带流水的渲染"资金流水"子表；⑤ 起诉书部分渲染量刑情节；⑥ 结论前新增**资金勾稽一览表** |
| 2 | **`generators/xlsx_catalog.py` 升级** | 新增"分卷总览"sheet（每卷一行 + `P起-止:文件名` 索引式提要，对齐律师版阅读习惯）；案件信息 sheet 增加资金勾稽与流水笔数 |
| 3 | **`vibelawyer/mcp_server.py`（核心交付物）** | FastMCP 封装，工具面设计（均带 `case_id`）：<br>· 生命周期：`create_case` / `list_cases` / `get_case_status`<br>· 读卷：`list_volumes` / `get_volume_outline` / `read_pages` / `search_volumes`<br>· 登记：`set_case_basic` / `record_party` / `record_indictment` / `add_charged_fact` / `record_statement` / `record_procedural_doc` / `record_documentary_evidence` / `add_transaction` / `add_catalog_entry` / `record_conclusions` / `record_funds_summary`<br>· 校验导出：`validate_citations` / `write_outputs` / `download_output`（File 返回 docx/xlsx）<br>· 全流程：`start_review`（后台线程跑 `run_case`，立即返回）/ `get_review_progress`（status+step+log 尾巴）<br>实现方式：薄封装——bind 会话后直接复用 tools.py 的 SDK 工具函数，零逻辑重复；job 运行期间拒绝他案调用 |
| 4 | **打包与文档** | `pyproject.toml` 加 `fastmcp>=3.4.5,<4` 依赖与 `vibelawyer-mcp` 入口脚本；README 补 MCP Server 用法（stdio 本机 / http 部署 / 鉴权建议 StaticTokenVerifier 或 JWTVerifier）；修正 CLAUDE.md 中失效的 python 路径 |
| 5 | **冒烟验证** | `.venv` 下：全模块 py_compile；合成 workspace（含全文/文号/流水/资金/分组）渲染 docx+xlsx 并断言关键结构；FastMCP app 实例化并列出工具清单 |

---

## 三、遗留事项与风险

- **无法本机端到端实测**：`data/` 卷宗 PDF 不在本机（gitignore），拿到数据后需重跑一次全流程，并对照律师版做**内容级 diff**（4 次讯问是否全提取、逐字全文质量、流水逐笔完整度、OCR 错字如"娄本娟/娄木娟"）。
- **token 成本上升**：逐字全文转录显著增加供述/证言提取的输入输出量，全流程费用预计上涨，跑真案前留意。
- **并发约束（v1）**：单进程同时只能跑一个全流程 job；要多案并发需后续改为每 job 独立子进程。
- **docling 缓存**：MCP 模式 `create_case` 不做预转换，首次 `start_review` 会一次性预转换（约 4 分钟/54 页），job 日志可见进度。
- 生成器改动尚未做（见未完成 #1/#2），目前 workspace 里的新字段**还不会出现在 Word/Excel 产物中**——这是下一步最关键的一环。

---

## 四、分发与上架（2026-08-01）

本地 stdio MCP → PyPI → mcp-marketplace.io **Free**。跟踪清单见根目录 `TODO-marketplace.md` 与 `LAUNCHGUIDE.md`。
